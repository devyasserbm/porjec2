name: nabih
description: "NABIH - Smart University Assistant (نبيه)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  supabase_flutter: ^2.9.0
  flutter_local_notifications: ^18.0.1
  json_annotation: ^4.11.0
  build_runner: ^2.13.1
  json_serializable: ^6.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
